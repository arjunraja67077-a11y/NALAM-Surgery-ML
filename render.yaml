services:
  - type: web
    name: nalam-surgery-ml-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python ml/train_model.py
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
